import { PipeInitialsPipe } from './pipe-initials.pipe';

describe('PipeInitialsPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeInitialsPipe();
    expect(pipe).toBeTruthy();
  });
});
