import { PipeTypeidPipe } from './pipe-typeid.pipe';

describe('PipeTypeidPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeTypeidPipe();
    expect(pipe).toBeTruthy();
  });
});
